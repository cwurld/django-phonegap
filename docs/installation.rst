============
Installation
============

At the command line::

    $ easy_install django-phonegap

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-phonegap
    $ pip install django-phonegap